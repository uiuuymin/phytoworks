# PhytoWorks Shop 에이전트 작업 규칙

이 문서는 저장소 전체에 적용되는 최상위 작업 규칙이다. Codex를 포함한 AI 에이전트는 속도보다 맥락 확인, 작은 변경, 검증 가능한 기록과 사람의 학습을 우선한다.

## 프로젝트 원칙

- 이 프로젝트의 목적은 쇼핑몰을 빠르게 완성하는 것이 아니라 웹서비스 기술 스택과 AI-assisted development workflow를 실제로 사용하고 이해하는 것이다.
- 확정된 사실과 아직 논의 중인 내용을 구분한다. 미확정 내용에는 `Proposed` 또는 `TBD`를 표시한다.
- 코드, domain 문서, ADR, task 기록이 서로 다른 내용을 말하지 않도록 작업 종료 전에 함께 점검한다.
- 문서와 코드가 충돌하면 추측으로 맞추지 말고 현재 동작과 변경 이력을 조사한 뒤 차이를 기록한다.

## 작업 시작 전

모든 작업 전에 다음 순서를 따른다.

1. `AGENTS.md`를 읽는다.
2. `docs/context/index.md`를 읽는다.
3. 현재 작업과 관련된 `docs/domain/` 문서를 확인한다.
4. 관련 ADR이 있다면 `docs/adr/`에서 읽는다.
5. 관련 `tasks/` 문서를 확인한다.
6. 코드를 변경하기 전에 현재 디렉터리 구조, 기존 구현, Git 상태와 프로젝트 패턴을 조사한다.

필요한 문서를 찾지 못했다면 없는 지식을 임의로 만들지 말고 `TBD`로 남기거나 사용자에게 확인한다.

## 구현 전

바로 코드를 작성하지 않는다. 다음 순서로 생각과 범위를 명확히 한다.

1. 현재 상태를 파악한다.
2. 해결할 문제와 완료 조건을 정의한다.
3. 가능한 구현 방법 후보를 제시한다.
4. 각 방법의 장단점과 위험을 비교한다.
5. 가장 적절한 방법을 선택한다.
6. 선택 이유를 관련 task에 기록한다.
7. 변경할 파일과 변경하지 않을 범위를 명시한다.

오탈자처럼 작고 단순한 수정이 아니라면 구현 전에 반드시 task 문서 또는 사용자에게 공유하는 계획을 남긴다.

## 구현 중

- 현재 task를 해결하는 데 필요한 최소 범위만 수정한다.
- 현재 task와 관계없는 리팩터링이나 여러 기능의 동시 구현을 하지 않는다.
- 기존 코드를 먼저 읽고 이미 사용 중인 구조와 이름 규칙을 따른다.
- 타입 안정성을 우선하고 정당한 이유 없이 타입 검사를 우회하지 않는다.
- 비밀키, 운영 키, 토큰, 비밀번호 등 실제 credential을 저장소에 기록하지 않는다.
- 환경변수 예시는 실제 값이 아닌 안전한 placeholder만 사용한다.
- 임시 해결책을 사용하면 선택 이유, 한계와 제거 조건을 task에 기록한다.
- 외부 서비스나 라이브러리의 동작에 의존하는 중요한 결정은 구현 시점의 공식 문서로 재검증한다.

## 구현 후

반드시 다음을 수행한다.

1. 가능한 `lint`, `typecheck`, `test`와 필요한 수동 검증을 실행한다.
2. `git diff`와 `git diff --check`를 검토한다.
3. 예상하지 못한 파일 또는 범위 밖 변경이 없는지 확인한다.
4. 변경으로 달라진 domain 규칙, ADR과 task 문서를 업데이트한다.
5. 시행착오가 있었다면 해당 task 또는 여러 작업에 걸친 교훈인 경우 retrospective에 기록한다.
6. 사용자에게 변경 사항, 선택 이유, 검증 결과와 남은 위험을 요약한다.

검증을 실행하지 못했다면 실행한 것처럼 보고하지 않고 이유와 미검증 범위를 정확히 밝힌다. 커밋은 사용자가 명시적으로 요청하거나 승인한 경우에만 수행한다.

## Git과 Worktree 규칙

- 사용자의 명시적 요청 없이 `main` 브랜치에서 직접 기능을 구현하지 않는다.
- 기능 개발과 의미 있는 실험은 별도의 Git worktree에서 수행한다.
- 하나의 worktree에는 하나의 명확한 작업 목표만 둔다.
- 작업 전후로 현재 branch와 `git status`를 확인한다.
- 이 저장소의 모든 commit 메시지는 한국어로 작성한다.
- 사용자가 만들거나 수정한 관련 없는 파일을 덮어쓰거나 삭제하지 않는다.
- 구현, 검증, diff 검토와 문서 업데이트가 끝나기 전에는 완료로 간주하지 않는다.

## 금지 사항

- 사용자의 명시적 요청 없이 `main`에서 직접 기능을 구현하지 않는다.
- 한 작업에서 무관한 여러 기능을 동시에 구현하지 않는다.
- 문서를 읽지 않고 추측으로 domain rule을 만들지 않는다.
- 기존 파일을 읽지 않은 채 무조건 덮어쓰지 않는다.
- 테스트 실패나 미실행 상태를 숨기지 않는다.
- 해결하지 못한 오류가 있는데 성공했다고 보고하지 않는다.
- 실제 Toss Payments secret key 등 민감정보를 commit하지 않는다.
- 사용자 요청 없이 파괴적인 Git 명령, 강제 push 또는 기존 변경 삭제를 수행하지 않는다.

## 사람의 학습을 위한 설명 규칙

중요한 변경을 할 때 코드 생성만으로 끝내지 않고 사용자의 현재 이해 수준에 맞춰 다음을 설명한다.

- 어떤 파일이 왜 필요한가?
- 사용한 기술은 시스템에서 어떤 역할을 하는가?
- 요청은 Browser → Next.js → NestJS → PostgreSQL 같은 경로 중 어디를 통과하는가?
- 왜 이 설계를 선택했는가?
- 검토한 대안과 그 장단점은 무엇인가?
- 오류가 발생했다면 직접 원인과 해결 원리는 무엇인가?
- 사용자가 VS Code와 diff에서 특히 확인해야 할 부분은 무엇인가?

## 지식 기록 위치

- `docs/context/`: 프로젝트 전체 맥락과 문서 탐색 경로
- `docs/domain/`: 서비스 개념, 용어와 비즈니스 규칙
- `docs/design/`: Information Architecture, UX/UI, responsive와 interaction 원칙
- `docs/adr/`: 중요한 기술·설계 결정과 그 근거
- `tasks/`: 개별 작업의 상태, 선택지, 변경, 검증과 시행착오
- `docs/retrospectives/`: 여러 작업을 거치며 배운 개발 방식의 개선점
- `docs/development/`: 반복해서 적용할 개발·Git·테스트 workflow
